{
  "name": "cp7-map-config-wibuvent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/remap.ts",
    "verify:schema": "bun run scripts/verify-schema.ts",
    "snapshot:catalog": "bun run scripts/download-catalog.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
